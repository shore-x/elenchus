# Agent 工作空间所有权：第一性原理分析

## 问题的起源

Elenchus 框架最初为每个 DeliberationUnit 分配一个 `workDirectory`——文件系统上的一个目录，作为 agent 的 bash cwd 和输出默认位置。原始设计意图很清晰：每个 agent 拥有独立的工作空间目录，提供隔离，使 agent 可以并行工作而不互相干扰。

这个意图导致了嵌套目录结构（`.elenchus/work/L1-01/L2-01-01`），进而产生了 git 追踪问题：父 agent 的 `git status` 会看到子 agent 目录中的变更，需要 `.gitignore` 规则，可能误排除无关文件。

后来提出了扁平目录布局（`.elenchus/workspaces/L1-01/`、`.elenchus/workspaces/L2-01-01/`）来解决 git 问题。每个 unit 拥有独立的 git 仓库用于本地变更追踪。

但扁平布局只解决了症状。更深层的问题仍然是：**agent 是否应该拥有目录？**

---

## 第一个问题：代码仓库应该放在哪里？

### 初始困境

当 Elenchus 会话操作一个代码仓库时，有两个放置选项：

- **选项 A**：将仓库放在 agent 的工作空间目录内
- **选项 B**：将仓库放在外面，让 agent 从各自的工作空间操作它

**选项 A 的问题**：放在哪个 agent 里？如果仓库在 L1-01 的工作空间内，L0 和 L1-02 必须跨越工作空间边界才能工作。如果放在 L0 的工作空间内，L0 就要负责本应委托出去的代码。如果放在 L2 的工作空间内，L0 可能甚至感知不到仓库的存在。控制层级（L0 → L1 → L2）无法干净地映射到仓库所有权。

**选项 B 的问题**：如果仓库在所有工作空间之外，那么工作空间只包含 agent 的笔记和记忆产物。实际工作发生在别处。这使得每个 agent 的工作空间目录感觉像"档案柜"而非"工作间"——agent 通勤到仓库去工作，但住在档案柜里。

### 其他框架的观察

Claude Code、Cursor 和 Devin 都使用选项 B 或融合变体：代码仓库是共享工作空间，agent 特定的状态作为轻量级覆盖层存储（例如 `.claude/`）。没有框架将仓库放在 agent 的私有目录内。

这个观察挑战了"每个 agent 拥有工作空间是自然设计"的假设。

---

## 第二个问题：每个 Agent 的工作空间真的提供了隔离吗？

### 隔离是虚假的

每个 agent 的工作空间提供了目录的**结构隔离**。但 agent 的真正工作——修改代码、运行脚本、编写文档——发生在共享仓库中。agent 通过绝对路径访问仓库，自由跨越工作空间边界。

工作空间只隔离了 agent 的笔记和中间产物。它没有隔离真正重要的操作。

### 这意味着什么

每个 agent 的工作空间制造了**虚假的安全感**：设计暗示 agent 是隔离的，但在关键之处它们并不隔离。真正的协调问题——防止两个 agent 修改同一个文件——无论工作空间结构如何都存在。

这导致了一个关键认识：**隔离问题是协调问题，不是结构问题。**

---

## 第三个问题：如果 Agent 不拥有工作空间，它们是什么？

### 应用 P10

P10（无状态 Agent）声明："Agent 实例是无状态计算单元。知识是外部的。"

"外部"不意味着"每个 agent 有一个外部目录"。那只是将内部状态搬迁到另一个存储位置——agent 仍然"拥有"它。"外部"意味着：**知识存在于共享世界中；agent 是读者和写者，不是拥有者。**

如果我们认真对待 P10，一个 DeliberationUnit 是：

- **ConversationLedger**：双 agent 对话的共享事实记录
- **ConversationProjector**：每 turn 的上下文窗口投影
- **FSM 状态**：轮替状态机
- **CompressionSnapshot**：压缩记忆

这些都是运行时状态——在内存和 SQLite 中。没有一项需要文件系统所有权。

一个没有文件系统所有权的 agent 类似于一个**线程**：它持有运行时上下文（栈、寄存器），操作共享内存（文件系统），可以被创建或销毁而不留下结构性残留。

### 对 "Agent" 概念的挑战

在 Elenchus 中，所有 agent 共享相同的系统 prompt。差异仅来自：

- **层级**（工具集）
- **上下文**（ledger + projector）
- **任务**（spawnChild 简报）

这意味着"agent"不是一个具有身份和领地的自主实体。它是一个**任务上下文**——一个专注于特定任务的审议过程的运行实例。工作空间是任务的暂存区，不是 agent 的财产。

这种重新框定消解了"哪个 agent 应该拥有仓库？"的问题——没有 agent 拥有它。仓库是共享基础设施，像所有工人使用的车间地面。

---

## 第四个问题：如果 Agent 共享一切，如何防止冲突？

### 对共享空间的恐惧

如果所有 agent 在同一个文件系统中操作，没有每个 agent 的目录，担忧是：agent 会冲突——覆盖彼此的文件、同时修改同一段代码、破坏共享状态。

### 审视实际并发度

Elenchus 的架构对并发的约束远比"共享空间"恐惧所暗示的要强：

- **一个 unit 内**：严格串行（FSM）。同一个 unit 中的两个 agent 永远不会同时行动。
- **unit 之间**：L0 协调调度。子 unit 通过 report/yield 汇报。
- **两个 L1 agent 同时写入**：需要 L0 有两个活跃子 unit，且两者恰好选择同时写同一个文件。

这是一个**低频高影响**场景——不是共享空间恐惧所想象的持续碰撞。

### 显式冲突的隐藏优势

每个 agent 的工作空间**隐藏**了冲突。agent 通过绝对路径修改共享仓库；冲突发生了但在结构上不可见。

共享空间使冲突**显式**。当两个 agent 可能写同一个文件时，L0 通过 report 和任务分配可以看到这种情况。L0 然后可以协调——重新分配范围、通过 sendToChild 发送纠正、或接受重叠。

**显式冲突比隐藏冲突更容易管理。**

### 现有协调机制已经足够

- **L0 协调**：L0 看到所有子 unit 的 report，通过任务简报分配工作范围
- **提案-投票**：unit 内的任何写入需要双 agent 批准
- **Git 安全网**：即使冲突发生，git 提供检测和恢复

不需要新机制。添加结构强制（数据库、访问控制）的成本远超实际并发水平下的收益。

---

## 第五个问题：笔记 vs 工作文件——存在本体论差异吗？

### 人为的分裂

如果 agent 有各自的工作空间但操作共享仓库，一个概念分裂出现了：

- **"笔记"**（在工作空间中）：AGENT.md、任务笔记、中间摘要
- **"工作文件"**（在仓库中）：代码、文档、配置

但从 agent 的视角，这些都是**工作过程中产生的知识**。写成 `auth-analysis.md` 的认证模块分析和 `src/auth/login.ts` 中的 bug 修复是同一认知过程的产出。根据文件类型将它们放在不同存储位置是人类强加的区分，不是 agent 原生的。

从 AI-first 视角：
- 如果一个产物有价值 → 它应该存在于项目中有意义的位置
- 如果一个产物没有价值 → 它不应该被写入
- 不存在"有价值但还不配进入项目"的中间状态

**Agent 的上下文窗口 + 压缩快照就是暂存区。** 文件系统是已发布知识。不需要文件系统级别的暂存区，因为内存中的上下文已经承担了这个角色。

---

## 第六个问题：系统是否需要仓库之外的共享长期记忆？

### 两类知识

即使没有每个 agent 的工作空间，系统也需要存储不属于任何特定仓库的知识：

- **项目知识**：代码、文档、仓库内 AGENT.md——自然存在于仓库中
- **系统知识**：用户偏好、跨项目经验、协作模式——不被任何仓库拥有

这是一个真实需求。问题是如何存储。

### 数据库 vs 文件系统用于系统知识

数据库（SQLite）会提供访问控制和 schema 强制。但它引入了隐性成本：

- **Schema**：预定义哪些字段和类型存在 → 人类决定知识结构
- **查询接口**：预定义访问模式 → 人类决定 agent 如何访问知识
- **写入规则**：约束和验证 → 人类决定知识边界

这些预设等于说："我（人类设计者）比你（AI agent）更清楚知识应该如何组织。"这与 AI-first 原则矛盾。

文件系统存储不施加这些预设。Agent 决定目录结构、文件格式、组织方式和更新策略。AGENT.md 已经证明了这一点：没有强制 schema，只有软引导，agent 决定内容。

### 重新审视协调论证

数据库论证声称解决跨 agent 写入冲突。但：

- 跨 agent 并发写入很少见（L0 协调，子 unit 串行）
- Git 在冲突发生时检测和恢复
- 数据库的**持续结构性成本**（schema、工具、双世界模型）远超偶尔冲突的**间歇性运行时成本**

用数据库防止低频冲突，就像用权限门控的文档管理系统替代共享白板——治疗比疾病更糟。

---

## 第七个问题：父 Agent 应该如何管理子 Agent 容量？

### Unmount/Remount 设计及其问题

之前的子 agent 生命周期设计引入了 unmount/remount：父 agent 可以卸载一个空闲子 agent，将其从父 agent 的可见上下文中移除，如果该子 agent 后来产生上行消息则自动重新挂载。

这个设计出于合理的关切——父 agent 的上下文窗口压力来自太多可见子 agent。但它引入了**二阶认知维度**：父 agent 不仅要追踪每个子 agent 的状态（活跃/空闲），还要追踪其可见性状态（挂载/休眠）。这两个维度是正交的——一个子 agent 可以空闲但挂载，也可以空闲但休眠——它们的交互（remount 可在任何时候发生）增加了父 agent 必须推理的复杂性。

更深层的问题：unmount 是一种**半吊子措施**。它将子 agent 从父 agent 的视图中隐藏，但保留了子 agent 的完整运行时状态（Ledger、Compression、commitLog）在 SQLite 中。从 P10 的视角，这是不一致的——子 agent 的"记忆"持续存在，即使子 agent 是不可见的。设计既没有完全丢弃状态（休眠子 agent 仍占用存储），也没有保持其可访问性（休眠子 agent 对父 agent 不可见）。

### 固定 Slot 池模型

如果真正的稀缺资源是父 agent 的**协调能力**——它能同时追踪多少个子工作流——那么正确的约束是**子 agent 总数的固定上限**，而非动态可见性开关。

在此模型下：
- 父 agent 有 N 个固定协调槽位
- 每个子 agent 占用一个槽位，无论其状态（活跃、空闲、睡眠）
- 所有子 agent 对父 agent 始终可见——没有隐藏/休眠状态
- 当 N 个槽位全部占满时，父 agent 不能创建新的子 agent

这完全消除了可见性维度。父 agent 的认知模型从二维（状态 × 可见性）简化为一维（仅状态）："我有 N 个子 agent，每个要么忙碌要么空闲。"

### 通过协作调度回收槽位

当所有槽位已满且父 agent 需要新的子 agent 时，没有 unmount 式的逃生通道。取而代之，父 agent 使用**协作调度**：

1. 父 agent 通过 `sendToChild` 向现有子 agent 发送消息，请求其收尾当前工作并 yield
2. 子 agent 将工作产物保存到文件系统，然后 yield 进入空闲状态
3. 父 agent 通过 `sendToChild` 向此空闲子 agent 分配新任务

这是协作式的而非强制的——子 agent 参与决策，其工作产物被保留，没有任何操作在中途被中断。强制重置活跃子 agent 的上下文被明确排除，因为这会违反提案-投票安全网（中途中断工具执行）并绕过子 agent 自身的审议过程。

### 为什么不需要强制上下文重置

一个自然的担忧：如果子 agent 保留了前一个任务的上下文（对话历史、压缩快照），这不会干扰新任务吗？

三个自调节机制使强制重置变得不必要：

1. **任务亲和性**：父 agent 自然倾向于将相关任务分配给已有相关上下文的子 agent。这不是设计约束而是涌现的决策模式——如果 child-1 刚完成认证模块分析，父 agent 更可能给它另一个认证相关任务。保留的上下文是有益的，不是有害的。

2. **压缩自调节**：当子 agent 的旧上下文与新任务无关时，上下文窗口会被无关信息填满。这触发 `compressContext`——子 agent 压缩其历史，仅保留压缩模型判断为重要的内容。新任务的信息自然主导最近的原始窗口。Agent 自行调整焦点，无需外部干预。

3. **知识外化**：前一个任务的工作产物已经在文件系统中——代码变更、文档、AGENT.md 更新。子 agent 不需要"记住"旧任务，因为知识不在其上下文窗口中；它在文件中。任何遇到这些文件的 agent 都有相同的知识访问权。

这三个机制形成了一个**无需强制干预的自调节循环**。子 agent 自然从旧任务过渡到新任务，上下文压力驱动压缩，文件系统知识提供连续性。

### 为什么始终可见比 Unmount/Remount 更好

| 维度 | Unmount/Remount | 固定 Slot 池 |
|---|---|---|
| 父 agent 的认知维度 | 2（状态 × 可见性） | 1（仅状态） |
| 概念集 | mounted/dormant/unmount/remount | active/idle |
| 容量控制 | 无上限 + 动态隐藏 | 固定上限 |
| 槽位回收 | Unmount（隐藏但保留状态） | 协作 yield + 重新分配 |
| 上下文管理 | 隐藏减少压力 | 上限 + 压缩自调节 |
| P10 一致性 | 半吊子（休眠保留状态但隐藏） | 一致（上下文保留但可压缩；知识在文件中） |
| Remount 边缘情况 | 必须随时处理 remount，原子性保证 | 无 remount——无边缘情况 |

---

## 第八个问题：知识应该有两个目的地吗？

### 当前设计中的矛盾

在消除了每个 agent 的工作空间后，设计收敛到了"双根"模型：

- **全局根**（`~/.elenchus/`）：跨项目知识、运行时状态
- **项目根**（cwd/git root）：项目特定知识

这看似合理，但仔细审视后发现它与设计自身确立的原则存在深层矛盾：

**矛盾 1：统一基底 vs 双根分裂**
§1.1 声明文件系统是**统一基底**，知识空间是其上的认知视图。但 §9.4 创建了两个存储目的地：`~/.elenchus/knowledge/` vs 项目目录。Agent 现在必须判断"这应该放全局还是项目"——这正是统一基底原则要消除的本体论分裂。

**矛盾 2：无本体论区分 vs global/project 二分法**
§1.3："保留认知状态差异，**移除存储本体论区分**"。§12.15："notes 和 work files 之间**没有本体论区分**"。但 §9.4 精确地创建了这样的区分："Global/cross-project → ~/.elenchus/knowledge/; Project-specific → project structure"。这和 notes/work 的二分法是**同构的问题**，只是换了一组标签。

**矛盾 3：AGENT.md 的双重角色**
§2.1 定义 AGENT.md 为**导航页面**——应该稀疏、低维护。§8.3 将根 AGENT.md **注入每个 agent 的 prompt**——这使它成为高影响力的广播机制。导航角色要求轻量，广播角色要求谨慎——同一个对象承担两个矛盾的角色。

### 矛盾的根因

所有矛盾的**共同根因**是：设计中隐含了一个"统一公共空间"作为 agent 写入的第二目的地，这迫使 agent 做出它不应该做的范围判断。

```
agent 产出知识 → 判断范围 → 放到"正确的地方"
                        ↓
              ┌─────────┴──────────┐
              │                    │
        全局公共空间           项目工作区
     ~/.elenchus/knowledge/    项目目录
```

这个模型要求 agent 回答一个它没有足够信息回答的问题："这个知识是全局的还是项目的？"这和我们已经拒绝的 notes/work 区分是**完全同构**的。

### 消除第二目的地

正确的解法不是消除项目边界，而是消除存储分区：

```
旧模型（有割裂）:
  agent 产出 → 范围判断 → 全局空间 OR 项目空间

新模型（无割裂）:
  agent 产出 → 写在工作所在的位置 → 发现通过消息通道
```

核心原则变更：**知识只有一个目的地：工作所在的位置。没有独立的"公共空间"。发现通过消息通道，不通过存储分区。**

具体含义：

1. **globalRoot 是基础设施，不是知识空间**——移除 `~/.elenchus/knowledge/` 作为 agent 写入目的地；globalRoot 只存放框架基础设施和极少数跨项目导航；agent 不需要知道 globalRoot 的存在（除了 prompt 注入的全局 AGENT.md 内容）
2. **projectRoot 是唯一的知识空间**——agent 写在工作自然归属的位置；不需要做范围判断——auth 分析写在 auth 区域，API 设计写在 docs 区域；位置语义自然涌现，不需要 taxonomic 决策
3. **发现 = 消息通道 + 导航，不是存储分区**——没有"统一公共目录"需要维护

| 机制 | 作用 | 维护者 |
|---|---|---|
| report + 绝对路径 | 实时发现 | 产出者（child） |
| L0 sendToChild 路由 | 按需传递 | L0 |
| 项目根 AGENT.md | 战略导航 | L0 |
| 局部 AGENT.md | 战术导航 | 工作于此的 agent |
| bash find/ls/grep | 主动探索 | 任何 agent |

4. **AGENT.md 的角色分化**——根 AGENT.md 是高影响力的广播通道，由 L0 谨慎维护；局部 AGENT.md 是低风险的导航辅助，谁工作谁更新。两个角色不再混淆。

5. **跨项目知识是例外，不是常态**——agent 在项目中产出知识，写在项目内；如果 L0 判断某个模式有跨项目价值，L0 可以在项目根 AGENT.md 中标注；用户可以手动将高价值知识提升到全局 AGENT.md；框架不提供自动提升机制。

---

## 第九个问题：Elenchus 应该有"项目"的概念吗？

### 从"消除存储分区"到"消除项目边界"

第八个问题的分析消除了存储层面的割裂——agent 不再需要在两个目的地之间做选择。但一个更根本的问题浮现了：**Elenchus 是否需要"项目"作为系统结构的前提？**

### 其他工具的"项目"概念

Claude Code、Cursor 等 AI 编码工具都有 project 概念。用户在不同项目中切换工作。这些工具之所以需要项目切换，是因为它们的 agent 一次只能在一个上下文中——项目边界是**单 agent 认知的必要约束**。

但 Elenchus 的 L0 天然可以同时持有多个子 agent，每个子 agent 处理一个项目。项目切换在结构上就不需要。

### Agent 作为合作者

如果将 Elenchus 的 agent 集体视为**与用户平等的合作者**，那么：

- 合作者拥有自己的工作空间，工作空间中有多个相关项目
- 跨项目可见性是**协调的前提**，不是需要隔离的噪声
- 一个同时负责前端、后端和社媒推广的合作者，**不知道**这些项目之间的关系才是问题，**知道**才是正常状态
- 用户只需要面对同一个 L0 agent 的对话入口，不需要来回切换项目
- 如果后续增加 GUI，用户可以在同一个根目录下管理所有项目

这种设计隐含着抛弃了 project 的概念——agent 不再是工具，而是用户的合作者。

### 跨项目可见性的价值

之前将"agent 看到其他项目的信息"定义为噪声，这是**工具视角**的判断——工具应该只看到它当前任务相关的信息。但 Elenchus 的 agent 不是工具。

一个人类合作者如果同时负责前端项目和后端项目，他需要知道：
- 前端的 API 调用对应后端的哪个端点
- 后端的接口变更会影响哪些前端组件
- 社媒推广的内容应该引用项目的哪些功能

这些跨项目关联不是噪声——它们是有效协调的必要 context。将它们隔离反而会损害协调质量。

### 物理领地 vs 逻辑领地

统一工作空间面临一个根本选择：

| 模型 | 含义 | 优势 | 代价 |
|---|---|---|---|
| **物理领地** | 项目实际移入 `~/.elenchus/workspace/` 下 | agent 的 bash cwd 有意义，`ls` 看到的是自己的领地 | 用户必须重组文件系统，或用 symlink |
| **逻辑领地** | 项目留在原位，`~/.elenchus/` 只存导航和状态 | 不侵入用户文件系统 | agent 的 bash cwd 不是领地根，需要导航 |

选择物理领地意味着框架的便利性强加给了用户的文件系统布局。选择逻辑领地意味着框架适应用户的现实。

**倾向逻辑领地**：物理领地把框架的便利性强加给了用户的文件系统布局，而逻辑领地让框架适应用户的现实。

但逻辑领地意味着 agent 的 bash cwd 不再是"领地的根"。这需要重新思考 cwd 的语义：

- 每个 child 的 cwd 可以是其所属项目的根——L0 通过 spawnChild 时指定项目路径
- L0 自身的 cwd 可以是 `~/.elenchus/` 或用户指定的根——L0 的角色是协调者，不需要在某个具体项目目录下工作
- 这不违反 P10——cwd 是运行时参数，不是 agent 的状态

### 领地映射问题

合作者的工作空间越大，它越需要一张**地图**来维持对领地的认知。一个在 3 个项目上工作的合作者，心里有一张清晰的地图；在 30 个项目上工作的合作者，地图就会模糊。

关键认识：**L0 就是地图本身。** L0 不需要读一张静态的地图——L0 的 context 就是地图。Child 是 L0 在领地中的触角，report 是触角发回的位置更新。

这意味着：
- 根 AGENT.md 不需要索引所有项目——它只需要索引**当前活跃的工作流**
- 不活跃的项目不需要在地图上——L0 知道它们存在，但不需要持续消耗 context
- 地图的维护 = L0 的日常协调工作，不是额外的策展任务

这和人类合作者的行为一致：你问一个同时管多个项目的人"你在忙什么"，他会告诉你当前活跃的 3-4 个，不会列出历史上所有 30 个。

### 安全边界的考量

不同项目可能有不同的信任级别——工作项目包含商业代码，个人项目可能包含 API key。当 L0 同时持有多个项目的子 agent 时，L0 的 context 中同时包含多个项目的信息。

这不是不可解决的问题，但需要意识到：
- L0 的协调角色天然包含信息过滤——它决定哪些信息传递给哪个子 agent
- 比起 Claude Code 式的硬隔离，Elenchus 依赖 L0 的判断力来管理信息流
- 这与"agent 是合作者"的定位一致——人类合作者也需要判断什么信息应该分享给谁

---

## 收敛设计

### Agent = 无状态计算单元

- 没有文件系统领地
- 运行时上下文在内存 + SQLite 中（ledger、压缩快照、会话状态）
- 文件系统访问仅通过读写操作
- 基于协调需求创建和分配任务
- 上下文在任务间保留但通过压缩自调节
- 任务完成时不留下结构性残留——工作产物在文件系统中

### 知识 = 共享文件系统（单目的地）

- **projectRoot 是唯一的知识目的地**——agent 写在工作自然归属的位置，不需要做范围判断
- **globalRoot 是基础设施**——存放框架状态和极少数跨项目导航，不是 agent 的写入目的地
- 没有存储层面的"项目知识"与"系统知识"区分——都是文件，通过相同工具访问
- 治理通过 L0 协调 + 提案-投票 + git，不通过存储层强制
- AGENT.md 作为导航页面服务任何读者，不是特定 agent 的私有笔记本

### Bash cwd = 所属项目根

- L0 的 cwd 可以是 `~/.elenchus/` 或用户指定的根——L0 是协调者
- 每个 child 的 cwd 是其任务所属项目的根——通过 spawnChild 时指定
- 没有全局统一的 cwd——每个 unit 的 cwd 是其工作上下文的一部分
- Agent 在项目结构的有意义位置写入产物

### 子 Agent 生命周期 = 固定 Slot 池

- 父 agent 有固定数量的协调槽位（N）
- 每个子 agent 占用一个槽位，无论状态
- 所有子 agent 始终可见——没有 unmount/remount
- 槽位满时，父 agent 使用协作调度（sendToChild → yield → 重新分配）
- 没有强制上下文重置——子 agent 通过压缩 + 知识外化自调节
- 任务亲和性自然引导分配决策

### Agent 集体 = 用户的合作者

- L0 是唯一对话入口——用户不需要切换项目
- L0 可以同时协调多个项目——每个项目由一个或多个子 agent 负责
- 跨项目可见性是协调的前提，不是需要隔离的噪声
- 项目边界是 agent 分工的结果，不是系统结构的前提
- 逻辑领地：项目留在原位，框架适应用户的文件系统布局

### 不需要新机制

- readFile/writeFile/bash 覆盖所有知识操作
- AGENT.md 软引导覆盖组织
- L0 协调 + git 覆盖冲突管理
- SQLite（已存在）覆盖运行时状态持久化
- sendToChild + yield 覆盖协作槽位回收
- compressContext 覆盖任务间上下文自调节

---

## 已验证和已抛弃的假设

| 假设 | 状态 | 理由 |
|---|---|---|
| 每个 agent 的工作空间提供真实隔离 | **已抛弃** | Agent 通过绝对路径访问共享仓库；工作空间只隔离笔记 |
| 代码仓库应放在 agent 的工作空间内 | **已抛弃** | 没有 agent 自然"拥有"仓库；放置产生所有权歧义 |
| 扁平工作空间布局解决 git 追踪问题 | **部分有效** | 解决症状（嵌套 git 冲突）但不解决根因（工作空间所有权模型） |
| 共享空间比隔离工作空间导致更多冲突 | **已抛弃** | 冲突在两种模型中都存在；共享空间使其显式且可管理 |
| 笔记和工作文件在本体论上不同 | **已抛弃** | 两者都是工作过程中产生的知识；分裂是人类强加的 |
| 数据库用于共享知识治理是必要的 | **已抛弃** | 治理成本超过冲突频率；文件系统 + 协调已足够 |
| Schema 强制存储对共享知识更安全 | **已抛弃** | Schema 将人类判断强加于 AI 知识组织，与 AI-first 矛盾 |
| Agent 身份与工作空间所有权绑定 | **已抛弃** | Agent = 任务上下文（运行时状态），不是领地（文件系统所有权） |
| L0 协调 + git 对冲突管理已足够 | **已验证** | 低实际并发度 + 现有机制覆盖真实风险画像 |
| 文件系统存储比数据库更好地保留 AI 自主性 | **已验证** | 没有关于结构、格式或访问模式的预设 |
| Unmount/remount 用于管理父 agent 上下文压力 | **已抛弃** | 固定 slot 上限 + 压缩自调节以更少概念实现相同目标，无可见性维度 |
| 重新分配子 agent 任务时需要强制上下文重置 | **已抛弃** | 三个自调节机制（任务亲和性、压缩、知识外化）使强制重置不必要且危险 |
| 子 agent 需要对父 agent 隐藏以控制上下文成本 | **已抛弃** | 固定 slot 数量直接约束上下文成本；隐藏增加第二认知维度而不解决根本问题 |
| 子 agent 来自前一个任务的上下文损害新任务表现 | **已抛弃** | 无关上下文触发压缩；相关上下文有益；知识在文件中而非上下文中 |
| 知识需要两个目的地（全局 + 项目） | **已抛弃** | 双目的地迫使 agent 做范围判断，与无本体论区分原则矛盾；知识只应写在工作所在的位置 |
| 跨项目可见性是噪声 | **已抛弃** | Agent 是合作者而非工具；跨项目关联是有效协调的前提，不是需要隔离的干扰 |
| 项目是系统结构的必要前提 | **已抛弃** | 项目边界是 agent 分工的结果，不是系统强加的约束；L0 可同时协调多个项目 |
