---
title: 从 Skill 与 Memory 的统一到文件系统认知底座：一次苏格拉底式推演
date: 2026-04-14
version: 1.0
---

# 从 Skill 与 Memory 的统一到文件系统认知底座：一次苏格拉底式推演

这不是一份正式设计文档，也不是一份方案说明书。

它记录的是一条推理链：我们如何从“skill 与 long-term memory 是否应统一”这个看起来局部的问题出发，经过一连串追问，逐步触碰到一个更大的命题——如果 agent 的工作世界本来就是文件系统，那么所谓知识空间，是否最终只是这个文件系统上的一种可导航认知视图？

这份文档不试图提前给出最终制度化答案。它只试图忠实记录这条推理链是如何一步步展开的：哪些直觉被保留，哪些经验主义假设被放弃，哪些结论暂时成立，哪些地方仍必须保留怀疑。

---

## 第一章 起点：为什么会想到统一 Skill 与 Memory？

最初的问题并不宏大。它来自一个很朴素的观察：`skill` 和长期记忆虽然在工程实现上通常被放在不同子系统中，但从 agent 的使用方式来看，它们常常承担的是相似的职能。

它们都不是当前轮次即时产生的对话内容，而是某种先验资源。

它们都可能在 agent 开始思考之前就已经存在。

它们都可能以自然语言的方式告诉 agent：

- 当前应该如何理解某个问题
- 某些经验结论来自哪里
- 哪些路径更可取，哪些风险值得注意
- 某些脚本、代码、项目文件应该如何被使用

于是第一个追问自然出现了：

> 如果 `skill` 与 `memory` 在 agent 看来都只是“可被调入的先验知识”，为什么必须把它们划分为两个本体上不同的对象？

这个追问并不是在否认历史上 skill 系统的合理性。传统 skill 系统之所以存在，是因为它通常承担了几项额外职责：

- 提供一种可安装、可分发的组织方式
- 在 prompt 中以较稳定的方式注入一段指导
- 有时还会注册工具或绑定脚本

但这些额外职责并不自动证明 skill 在本体上就不是知识。它们只说明 skill 在很多系统里被赋予了额外的工程包装。

一旦把这些包装层剥离，问题就变得尖锐起来：

> skill 的核心是否不过是“被组织过的、可渐进披露的知识”？

如果答案接近“是”，那么统一的方向就出现了：skill 与 memory 不必继续被实现为两套先验上不同的对象，它们完全可能只是同一知识空间中的不同节点、不同入口、不同组织方式。

这一步还相对保守。此时我们讨论的“统一”，仍然停留在自然语言文本、引用关系、脚本附件、渐进披露这些比较熟悉的层面。也就是说，知识空间在这个阶段仍然像是一个更宽松、更 AI-native 的 skill/memory 容器。

但真正重要的是，这一步已经埋下了后面所有外推的种子：

> 只要某样东西能够在后续推理中被再次读取、解释、引用，并对 agent 的行动产生影响，它就开始具备“知识”的性质。

---

## 第二章 第一次追问：如果 Skill 可以带脚本，那么中间结果文件算不算知识？

一旦接受了上面的起点，问题立刻向前迈了一步。

如果一个 skill 不再被理解为“必须注册 tool 的特殊插件”，而是被理解为“知识文本 + 若干被引用的脚本/代码文件”，那么脚本文件本身显然已经成为知识空间的一部分。至少，它们不再只是外在于知识系统的附属物，而是与知识文本共同构成一个可被 agent 理解和调用的整体。

这时第二个追问出现了：

> 如果脚本文件可以是知识的一部分，那么 agent 在执行过程中生成的中间结果文件，为什么不可以被视为知识？

例如：

- 一次检索后整理出来的摘要
- 一个对比分析的临时表格
- 一段实验性脚本的输出结果
- 某个目录下为后续任务准备的检查清单

这些东西与“长期记忆”之间的区别，并不在于它们是否能影响未来推理，而往往只在于它们的生命周期更短、稳定性更低、表达可能更粗糙。

换句话说，它们不是“不是知识”，而是“尚未被整理、尚未被沉淀、尚未被提升为更稳定知识入口的知识候选”。

因此，第一道经验主义边界开始动摇：

> 知识并不只等于那些被人工正式命名为 skill 或 memory 的文件。

只要某个外部文件承载了可复用的认知成果，它就已经进入了知识的外延。

这一步的意义很大。因为一旦承认中间结果也属于知识，就意味着知识不再只是“被预先准备好的静态资源”，而开始包括 agent 在工作过程中不断外化出来的认知产物。

知识空间于是第一次从“存储先验知识的地方”变成了“承载 agent 外部化认知痕迹的地方”。

---

## 第三章 第二次追问：如果中间结果是知识，那么下载文件算不算知识？

继续沿着同样的逻辑追问，边界会进一步松动。

假设 agent 从网络下载了某个代码仓库、某篇文档、某个规范 PDF、某个数据集。起初我们也许会本能地认为，这些只是“外部资源”，还不是“知识”。

但这个判断其实混入了一种未经检验的经验主义预设：

> 只有被系统正式整理、筛选、摘要过的内容，才能被叫做知识。

这个预设看起来合理，但并不来自第一性原理。

从 agent 的视角看，一个下载来的文件与一个本地已有的文件并没有本体差异。只要它们都能被读取、解释，并可能在后续推理中起作用，它们都属于同一个可访问的资源空间。

于是问题变成：

> 下载文件是否“已经被纳入知识视图”，而不是它是否“本体上属于知识”。

这一区分非常关键。

如果一个下载文件只是被动躺在磁盘上，agent 从未再引用它，也没有任何导航关系指向它，那么它当然只是一个潜在认知资源。

但如果这个文件开始被：

- 在总结中引用
- 在后续推理中反复访问
- 被某个目录导航页标记为关键来源
- 被压缩进某个更稳定的摘要

那么它事实上就已经被提升成知识空间中的一个活跃节点。

因此，第二道经验主义边界也被削弱了：

> “下载来的东西只是资源，不是知识” 这个说法并不稳固。

更准确的说法是：

> 下载来的东西首先是新进入文件系统的外部资源，而后可以在 agent 的认知过程中被提升为知识空间中的活跃部分。

这一步继续扩大了知识的外延，但它也开始带来焦虑：如果连下载文件都可能是知识，那么边界到底在哪里？

---

## 第四章 第三次追问：子 Agent 的临时工作区算不算知识？

推理继续深入时，一个更敏感的问题出现了。

子 agent 在执行任务时，往往会建立自己的临时工作区，里面可能有：

- 中间脚本
- 一次性实验结果
- 粗糙的分析草稿
- 尚未提交的半成品结论
- 仅服务于局部任务推进的辅助文件

直觉上，人很容易说：这些太临时、太脏、太局部了，应该算工作痕迹，不该算知识。

但如果我们坚持前面已经承认的原则——只要某个文件可能在未来被再次读取、解释并影响决策，它就具有知识性质——那么临时工作区也很难被排除在外。

事实上，临时工作区与长期知识的差别，更多是：

- 生命周期不同
- 可靠性不同
- 组织程度不同
- 进入默认上下文的优先级不同

而不是本体上属于完全不同的种类。

这一点很不舒服，但正因为不舒服，它才有哲学上的价值：

> 它迫使我们承认，“知识”与“工作产物”之间并不存在一条天然明确、永恒稳定的边界。

很多系统之所以还能维持这条边界，主要依赖的是人为制度：

- 这个目录被叫做 memory，所以它是知识
- 那个目录被叫做 tmp，所以它不是知识
- 这个文件被人工整理过，所以它值得保留
- 那个文件太临时，所以不必进入认知空间

这些判断在工程上当然有价值，但它们不是第一性原理结论，而是人为治理约定。

当我们试图设计一个更 AI-native 的系统时，就不得不面对这个更不舒服但更基本的事实：

> 对 agent 来说，真正原初的不是“知识文件”和“非知识文件”的二分，而是“可访问的外部文件系统”。

---

## 第五章 第四次追问：如果不断外推，是否必须承认整个文件系统都是知识？

到了这里，一个几乎不可避免的结论浮现出来。

如果：

- skill 可以是知识
- memory 可以是知识
- 脚本可以是知识的一部分
- 中间结果文件可以是知识
- 下载文件可以进入知识视图
- 子 agent 的临时工作区内容也可能在未来被提升为知识

那么继续追问下去，最终似乎只能得到一句更极端的话：

> 整个文件系统就是 agent 的知识空间。

这个判断之所以诱人，是因为它确实抓住了某种深层统一性。它不再把知识看作一个与真实工作空间平行的第二存储系统，而是承认：agent 的工作世界本来就以文件系统为外部底座。

从这个角度看，“知识系统”之所以显得必要，往往只是因为文件系统缺少：

- 足够清晰的局部语义说明
- 明确的导航入口
- 稳定的引用关系
- 能被 agent 持续维护的组织层

也就是说，问题不再是“有没有知识”，而是：

> 这些外部资源之间是否具有足够好的可导航性与可解释性。

在这个意义上，说“整个文件系统都是潜在知识”是成立的。

但如果进一步说“因此无需再定义知识这个概念”，问题就开始变得危险。

因为这里有一个隐藏的跳跃：

- 从“所有外部文件都可能成为知识”
- 跳到了“知识这个概念已经失去意义”

这一步并不自动成立。

---

## 第六章 关键反驳：为什么不能直接取消“知识”这个概念？

如果彻底取消“知识”这个概念，文件系统当然还在，agent 当然仍可访问一切文件。但这样做会失去一个非常重要的认知层区分。

因为“知识”这个概念在这里未必是为了定义某个独立存储系统，而是在定义一种特殊的认知视角：

> 哪些外部资源，正在被 agent 当作可解释、可引用、可维护、可逐步组织的认知对象来对待？

这与“文件是否存在于磁盘上”不是同一个问题。

磁盘上可能同时存在：

- 项目核心设计文档
- 构建缓存
- 未完成的下载碎片
- 一次性日志
- 无意义的临时文件
- 经过讨论沉淀出的高价值摘要

如果完全取消“知识”这个概念，那么这些东西在理论上都只是“文件”。

这当然在存储层面没错，但在认知层面却太弱了。因为 agent 仍然需要一种方式去区分：

- 哪些内容值得持续维护
- 哪些内容只是暂时存在
- 哪些内容应作为默认入口
- 哪些内容只在特定任务下才值得展开

于是，一个更稳的表述浮现出来：

> 文件系统是外部资源底座；知识空间不是与之并列的第二存储，而是这个底座上的一种可导航认知视图。

这句话保留了两件同样重要的事实：

第一，知识空间不应再被做成脱离工作区文件系统的独立孤岛。

第二，知识这个概念仍然有必要，因为它表达的不是“文件是否存在”，而是“文件是否已被纳入 agent 的认知治理视野”。

换句话说，`knowledge` 在这里不再是一个本体论上封闭的对象类别，而是一个认知论上的组织视角。

这一步非常关键，因为它防止整个思路滑向一种看似极简、实则失去操作性的口号：

> 一切都是知识。

更准确的说法应该是：

> 一切可访问文件都可能成为知识，但只有一部分外部资源会在当前阶段被纳入知识视图、被赋予更高的认知地位。

---

## 第七章 第五次追问：如果文件系统是底座，那么知识空间靠什么组织？

一旦承认文件系统是统一底座，新的问题随之而来。

如果不再另建一套独立知识系统，那么 agent 如何在这个广阔而混杂的文件系统中找到方向？

这时，目录导航页的想法浮现出来。

设想在某些目录下存在一个本地 markdown 文件，它不承担方案配置职责，也不被硬编码成严格 schema，而只是用自然语言说明：

- 这个目录是做什么的
- 哪些子目录最重要
- 哪些文件是入口
- 哪些文件只是缓存或临时产物
- 如果 agent 要理解这里，推荐先读什么
- 与别处的哪些目录或文件存在强关联

这实际上是在给文件系统增加一层局部语义入口。

它没有试图取代目录结构本身，而是在目录结构之上补了一层 agent 可读、可写、可演化的导航说明。

这一步之所以有吸引力，是因为它把问题从“如何定义知识对象”转移到了“如何让真实工作空间具备足够好的可导航性”。

这比设计一个抽象的中央知识库更贴近 agent 的真实工作流。

也正是在这里，整个推理开始出现一个暂时收敛的形态：

> 也许所谓 knowledge space，并不是一个独立的容器，而是文件系统在被导航页、引用关系、局部摘要与 resident 入口逐步组织之后，所形成的可导航认知空间。

这是一个很强的直觉，因为它几乎消除了“知识库”和“工作区”之间的割裂。

---

## 第八章 再次反驳：为什么不能要求每个目录都有导航页？

然而，正是在这个最诱人的时刻，必须进行一次反驳。

如果目录导航页这么好，最简单的想法似乎是：

> 那就让每个目录都拥有一个导航页，并由 agent 维护它。

这一步看起来几乎是顺理成章的，但仔细推敲就会暴露问题。

因为这实际上意味着：

- 每个目录都要被语义化
- 每个目录都要被维护
- 每个目录都要承担局部元信息同步责任
- 文件系统的表层结构会迅速被大量导航页覆盖

一旦这种要求被普遍化，系统会立刻出现几个问题。

第一，很多目录并不值得被长期语义化。

例如 agent 从网络上下载一个大型代码仓库，并没有任何必要为其中的每一个目录都补写导航说明。绝大多数目录对当前任务并不关键，它们最多只是可搜索、可按需理解的局部区域。

第二，导航页本身也会漂移、腐烂、失真。

如果目录内容变化很快，而导航页没人更新，那么它很快就会从“帮助理解的语义入口”退化为“制造误导的陈旧注释”。

第三，agent 可能会把大量精力消耗在维护这些导航页本身，而不是完成真正的任务。

于是，一个重要修正出现了：

> 目录导航页应被理解为高价值区域的局部语义入口，而不是整个文件系统必须机械覆盖的层。

这一步非常关键，因为它把思路从一种形式主义极端拉了回来。

也就是说，整个文件系统可以原则上属于统一认知底座，但只有其中的一部分会被进一步“语义化”为高质量的导航区域。

这一区分并不是对统一性的背叛，反而是它得以成立的前提。

如果没有这一步收缩，所谓统一知识空间最终只会退化为“给所有目录加说明文档”的维护地狱。

---

## 第九章 第一性原理上被排除的几个经验主义假设

回看整条推理链，可以发现我们一步步放弃了几个起初很自然、但经不起追问的假设。

第一个被放弃的假设是：`skill` 与 `memory` 在本体上天然不同。

经过追问后，它们更像是同一外部先验知识在不同组织方式下的表现，而不是必须分裂成两个独立物种。

第二个被放弃的假设是：只有正式整理过的长期内容才配叫知识。

一旦承认脚本、中间结果、下载文件、临时工作区内容都可能进入未来推理，这个假设就不再稳固。

第三个被放弃的假设是：知识有一条先验确定的边界，而文件系统之外另有一个独立知识库。

更接近第一性原理的看法是：agent 原初面对的是整个外部文件系统；知识空间只是这个底座上逐步被建立起来的认知组织层。

第四个被放弃的假设是：如果统一到文件系统，就应当立即对整个文件系统进行全面语义化。

这一步被证明并不合理，因为它忽略了导航页本身的维护成本、漂移风险和注意力消耗。

经过这些排除后，剩下的不是一个“更完整的经验模板”，而是一个更朴素但更稳的骨架：

- 外部文件系统是 agent 的统一工作底座
- 知识不是另一套存储，而是底座上的认知视图
- 导航、引用、摘要与 resident 入口构成这层视图的组织手段
- 这种组织应优先覆盖高价值区域，而非机械覆盖全部目录

---

## 第十章 当前可成立的暂时结论

经过这一轮苏格拉底式推进，当前最能经受质询的表述，不是“skill 与 memory 统一成一个知识库”，也不是“整个文件系统就是知识，因此知识这个词可以废除”，而是下面这个更克制的版本：

> Elenchus 的 knowledge space 不应被实现为脱离工作区文件系统的独立存储，而应被理解为 agent 对整个可管理文件系统建立起来的可导航认知视图。

在这个表述下，几个关键点同时被保留下来：

第一，`skill`、`memory`、脚本、中间结果、下载资料、子 agent 工作区内容，都属于同一外部资源空间，而不是几个互不相干的对象家族。

第二，`Resident Knowledge` 仍然有意义，但它不再意味着一个封闭的小宇宙，而是这个更大认知空间中的默认常驻入口视图。

第三，目录导航页是一种非常有前景的组织手段，但它只应服务于值得长期语义化的局部区域，而不应被推广成“每个目录都必须有”的制度。

第四，知识膨胀、漂移、腐烂、冲突整理、临时产物何时提升或清理等问题，并没有在这条推理链中被解决。它们只是从一开始就被推到了更准确的位置：

> 它们属于后续的 knowledge anti-entropy 问题，而不是当前统一知识空间时必须先行假装解决的问题。

---

## 结语：为什么这条推理值得保留？

这条讨论之所以值得被单独记录，并不因为它已经产出了最终制度，而是因为它做对了另一件更重要的事：

它没有急着寻找一个熟悉的工程模板，然后把 skill、memory、文件系统、临时工作区强行塞进那个模板；相反，它不断追问每个边界是否真的必要，哪些区分只是历史实现留下的惯性，哪些概念在更高一层抽象下其实可以收拢。

这正是 Elenchus 方法的价值：

- 不是先假定分类再填空
- 而是先不断质询分类本身是否成立
- 不是先把经验主义制度当作真理
- 而是先追问这些制度背后的前提是否真的稳固

经过这轮追问，当前最值得保留的成果不是某个具体文件格式，也不是某种固定目录布局，而是一种更清晰的认识：

> agent 的真实工作世界，本来就是文件系统；所谓知识空间，并不是去创造第二个世界，而是在这个世界之上，逐步建立起可导航、可解释、可维护的认知秩序。

这不是终点，但它已经足够构成一个新的出发点。
